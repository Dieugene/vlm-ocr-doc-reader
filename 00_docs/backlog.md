# Backlog

## Открытые

- **Проверка на больших документах.** Текущая верификация — только на `03_data/test_document.pdf` (8 стр). Нужны 30–100+ стр., разные типы (регуляторные, финансовые, научные). В частности проверить устойчивость Level 2 voting при большом объёме.
- **Юникод-порча в сохранённых значениях.** В `state.json` видны `�` (replacement character) в значениях с кириллицей (см. `04_logs/e2e_diag/`, `04_logs/e2e_verify/`). Предположительно где-то в OCR-клиенте декодирование с `errors='replace'` по cp1252. Баг существует до этапа 5.
- **Параметризация осей verify.** Сейчас единственная реальная ось — `chunk_size`. Добавить опционально варьирование DPI, порядка промптов внутри чанка, модели (второй OCR-провайдер), если эмпирика покажет, что `chunk_size` даёт слишком коррелированные ошибки.

## Справочно

E2E на `03_data/test_document.pdf` (8 страниц): scan извлекает текст (~16K), headers (40+), OCR Registry (~30 атомарных записей). Resolve по multi-question (chunk_size=5) — 10 OCR-вызовов вместо 32, ~1 мин wall-time. Verify по axes=[1,3,5] — 56 OCR-вызовов (32+14+10), ~1 мин wall-time, на выходе confidence-гистограмма (в референсном прогоне: 14 `3/3`, 16 `2/3`, 1 `1/3`, 1 `1/1`). Эмпирика через `scripts/ocr_chunk_grid.py` и обычный `verify` CLI.
