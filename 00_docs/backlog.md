# Backlog

Активных задач нет.

Следующий шаг — E2E-диагностика текущей реализации (`DocumentReader.scan` + `resolve` на реальном документе) как вход в новую фазу разработки. Наблюдавшееся расхождение в `02_src/demo_workspace/` (page_states покрывает не все страницы, registry содержит сущности для страниц без статуса, все `resolution=0`) — повод для первой диагностики.
