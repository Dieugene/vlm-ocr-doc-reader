# Backlog

## Открытые

- **Проверить scan/resolve на более объёмных документах.** Текущая верификация — только на `03_data/test_document.pdf` (8 страниц). Нужны документы 30–100+ страниц, желательно разных типов (регуляторные, финансовые, научные). Следить за: качеством распределения сущностей по страницам, частотой `no_data`, временем прогона, размером контекстного окна при batch.
- **Misattribution VLM между страницами в batch.** Явный маппинг «изображение #N — страница M» в `user_prompt` (reader.py:160) уменьшил, но не устранил неправильную привязку полностью. На `test_document.pdf` одна сущность («минимальный процент free float по ASX Listing Rules») стабильно приписывается странице 1, хотя физически на странице 2. Возможные направления: запросить `ocr_registry` в структуре, сгруппированной по страницам (вариант 2 из анализа); либо фолбэк `batch_size=1` для точности; либо постфильтр с проверкой OCR и переносом сущности на соседнюю страницу при `no_data`.
- **Слишком общие prompt'ы от VLM.** Qwen иногда добавляет сущности вида «найди пример числа» без конкретики — OCR на такие стабильно возвращает `no_data`. Уточнить формулировку в `SCAN_PROMPT_TEXT`, чтобы требовать либо контекста сущности, либо имени признака.

## Справочно

E2E на `03_data/test_document.pdf` (8 страниц, `qwen3-vl-flash` + `qwen-vl-plus`): scan извлекает текст (~16K), headers (40+), OCR Registry (~80–100 записей). Resolve покрывает весь registry за 10–15 минут. Доля `no_data` — 4–5% при текущем промпте, сосредоточена на одной странице из-за misattribution.
