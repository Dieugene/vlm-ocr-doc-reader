# Environment Setup

## Python Projects

Всегда работай в виртуальном окружении (venv/virtualenv).

## Environment Variables

Для работы с переменными окружения используй `.env` файл.

**Обязательно:** Добавь `.env` в `.gitignore` чтобы не коммитить секреты.
