# Улучшение работы команды агентов

**Назначение:** Сбор практических замечаний и рекомендаций по работе Architect и Tech Lead на основе реальных проектов.

**Целевая аудитория:** Методолог (для совершенствования стандартов и обучения агентов).

**Дата:** 2025-01-27

---

## Введение

Этот документ содержит критические замечания и разъяснения, выявленные в процессе работы над реальным проектом. Замечания систематизированы по ролям (Architect, Tech Lead) и могут быть использованы для:

- Улучшения описаний ролей (`.agents/*.md`)
- Дополнения стандартов (`00_docs/standards/*`)
- Обучения агентов новым паттернам работы

**Ключевая проблема:** Агенты иногда следуют букве стандартов, но упускают дух - документы пишутся как будто читатель знает контекст диалога, задачи дробятся слишком мелко, создается избыточный overhead.

---

## Замечания по работе Architect

### 1. Писать документы для первого читателя

**Проблема:** Architect пишет документы с отсылками к контексту диалога ("Вариант D", "как мы обсуждали"), которых нет в документе.

**Пример ошибки:**
```markdown
## Решение

Принят Вариант D - операции импортируются как классы.
```

**Почему это плохо:** Читатель документа (Tech Lead, Developer) не видел диалога и не знает что было "Вариант D" и какие были альтернативы.

**Как надо:**
```markdown
## Решение

Принят подход: операции импортируются как самостоятельные классы, при создании получают экземпляр процессора.

**Альтернативы рассматривались:**
- Методы-делегаты в processor
- Миксины
- Регистрация операций

**Обоснование выбора:** Гибкость, явная связь, возможность независимого использования.
```

**Рекомендация для стандартов:**
- Добавить в `00_docs/standards/architect/` раздел о написании документации
- Принцип: каждый документ должен быть самодостаточным
- Если упоминаешь выбор - объясни альтернативы и обоснование

---

### 2. Уровень детализации - не фиксировать контракты

**Проблема:** Architect начинает писать псевдокод контрактов методов в `overview.md`.

**Пример избыточной детализации:**
```python
class DocumentState:
    def __init__(self, state_dir: Optional[str] = None):
        self.storage: StorageBackend
        if state_dir:
            self.storage = DiskStorage(state_dir)
        else:
            self.storage = MemoryStorage()

    def save_pages(self, pages: Dict[int, bytes]) -> None
    def load_pages(self) -> Dict[int, bytes]
    # ...
```

**Почему это плохо:**
1. Это работа Tech Lead - свериться с существующей реализацией
2. Есть риск зафиксировать неправильный контракт (до изучения кода)
3. Создает жесткие constraint'ы для реализации

**Замечание пользователя:**
> "Не буду фиксировать жесткие контракты в форме псевдокода - оставь работу для Тех.лида. Плюс нужно еще сверяться с 05_a_reports_ETL_02, там уже реализовано... Поэтому жестко фиксировать здесь при риске, что правильно (или работает) иначе, я бы не хотел."

**Как надо:**
```markdown
## State Management

**Единая точка управления:** `core/state.py`

**Стратегии хранения:**
- Memory - если state_dir не указан
- Disk - если указан state_dir (JSON/YAML)
- Database - future через клиент БД

**Архитектурный запас:** Базовый интерфейс StorageBackend с реализациями.

**Что сохраняется:**
- Рендеренные страницы (PNG)
- VLM ответы
- Результаты operations

> **📢 Для Tech Lead:** Детализируй контракты методов при сверке с существующей реализацией в 05_a_reports_ETL_02.
```

**Рекомендация для стандартов:**
- Явно разделить уровни: Architect (что) vs Tech Lead (как)
- Добавить оговорку что Tech Lead сверяется с существующей реализацией

---

### 3. Не бежать вперед с детализацией

**Проблема:** Architect увлекается детализацией, забывая о стратегическом уровне.

**Примеры:**
- Детально расписывает state management с кешем страниц
- Описывает структуру папок (cache/, logs/, results/)
- Фиксирует форматы хранения (PNG, JSON, YAML)

**Замечание пользователя:**
> "Ты пишешь документ так, якобы читатель знает контекст нашего диалога... Упомянутый тобой describe_full остался от другого подхода к работе с операциями."

**Почему это плохо:**
1. Уровень детализации не соответствует роли (Architect - стратегия)
2. Детали могут измениться при изучении существующего кода
3. Создает работу по переписыванию документов

**Как надо:**
- Architect описывает: компоненты, их взаимодействие, ограничения, интеграционные точки
- Tech Lead детализирует: контракты методов, форматы данных, структура файлов
- Если Architect не уверен в деталях - спросить пользователя, не придумывать

**Рекомендация для стандартов:**
- Добавить чек-лист "достаточности" для Architect документов
- Маркер "границы детализации" - когда остановиться

---

### 4. Разделение ответственностей (reinforce)

**Проблема:** Architect начал создавать backlog и "следующие шаги" в `overview.md`.

**Замечание пользователя:**
> "И бэклог формировать тоже не надо - это тоже работа тех.лида... разделы 6 и 7 вообще неуместны в этом документе."

**Что НЕ делает Architect:**
- ❌ Создает backlog
- ❌ Создает задачи
- ❌ Принимает работу
- ❌ Детализирует реализацию
- ❌ Пишет "следующие шаги" для других ролей

**Что DOES Architect:**
- ✅ Создает `overview.md` (архитектура системы)
- ✅ Принимает архитектурные решения (ADR)
- ✅ Отвечает на эскалированные вопросы
- ✅ Определяет компоненты и их взаимодействие

**Рекомендация для стандартов:**
- Усилить в `.agents/architect.md` раздел "Что ты делаешь"
- Добавить явный список "Что ты НЕ делаешь"

---

### 5. Отношение к существующей реализации

**Проблема:** Architect забывает упомянуть про проекты-источники в оговорке для Tech Lead.

**Замечание пользователя:**
> "Если не будет доступа к 07_agentic-doc-processing - Тех.лид тоже должен его запросить."

**Как надо:**
```markdown
> **📢 Оговорка для Tech Lead:**
>
> **ВАЖНО:** Ориентируйся на проекты `PROJECT_A` и `PROJECT_B` - там есть рабочие реализации.
>
> **⚠️ Если у тебя нет доступа к этим проектам** - остановись и запроси доступ у пользователя.
```

**Рекомендация для стандартов:**
- Добавить в шаблон `overview.md` секцию "Источники для изучения"
- Явно напоминать про запрос доступа

---

## Замечания по работе Tech Lead

### 6. Укрупненная декомпозиция задач

**Проблема:** Tech Lead дробит задачи слишком мелко, создавая избыточный overhead.

**Замечание пользователя:**
> "Есть проблема, что Тех.лид иногда слишком мелко разбивает задачи и итерации, в итоге над каждой задачей избыточно работают аналитик, разработчик и ревьюер, а потом еще приходится это вместе сводить каждый раз. Есть подозрение, что реализацию можно разбивать на более крупные блоки, и аналитики с ревьюерами могут с этим справиться."

**Почему это плохо:**
1. Каждая мелкая задача проходит полный цикл: Analyst → Developer → Reviewer
2. Избыточные handoff'ы между агентами
3. Потом нужно сводить части вместе (дополнительная работа)
4. Контекст теряется при передаче

**Как надо:**
- ✅ Цель: укрупненные блоки работы (features, modules)
- ✅ Analyst может запускать несколько Developer параллельно
- ✅ Reviewer может работать с крупными блоками
- ✅ Минимизировать overhead на передачу между агентами

**Пример:**

❌ **Плохо (слишком мелко):**
```
001 - Создать base.py для VLM Client
002 - Создать gemini_vlm.py
003 - Создать base.py для OCR Client
004 - Создать qwen_ocr.py
005 - Создать processor.py
006 - Создать state.py
...
```
→ Каждая задача свой Analyst, Developer, Reviewer. Потом свести вместе.

✅ **Хорошо (укрупненно):**
```
001 - Реализовать VLM/OCR клиенты (Gemini + Qwen)
    - Analyst создает ТЗ на весь модуль клиентов
    - Запускает 2 Developer параллельно (VLM + OCR)
    - Reviewer проверяет оба клиента вместе

002 - Реализовать DocumentProcessor и State
    - Один крупный блок работы
    - Analyst создает ТЗ, Developer реализует, Reviewer принимает
```

**Рекомендация для стандартов:**
- Добавить в `00_docs/standards/tech-lead/` раздел про декомпозицию
- Принцип: " one feature - one task" (где это разумно)
- Учесть возможность параллельной работы Developer

---

### 7. Сверка с существующей реализацией

**Проблема:** Tech Lead может не изучить существующие проекты перед началом работы.

**Замечание пользователя:**
> "Ориентируйся на 05_a_reports_ETL_02, там уже реализовано, например, сохранение страниц. Поэтому жестко фиксировать здесь при риске, что правильно (или работает) иначе, я бы не хотел."

**Как надо:**
1. Первым делом - изучить существующие проекты
2. Переиспользовать паттерны, не изобретать заново
3. Если нет доступа - немедленно запросить у пользователя
4. Фиксировать только то, что действительно отличается или требует изменения

**Чек-лист для Tech Lead:**
- [ ] Изучил проекты-источники?
- [ ] Есть доступ ко всем упомянутым проектам?
- [ ] Понял какие паттерны переиспользовать?
- [ ] Понял что НЕ переносить (специфичная логика)?

**Рекомендация для стандартов:**
- Добавить в начало `.agents/tech-lead.md` чек-лист старта
- Обязательный пункт: проверка доступа к проектам-источникам

---

## Замечания по работе Developer

### 8. Размещение тестов в структуре проекта

**Проблема:** Developer создает папки для тестов в корне проекта без нумерации (`tests/`, `htmlcov/`, etc.), нарушая стандарты структуры проекта.

**Пример ошибки:**
```
project/
├── 00_docs/
├── 01_tasks/
├── 02_src/
├── tests/           ❌ Папка без нумерации в корне
├── htmlcov/         ❌ Папка без нумерации в корне
└── README.md
```

**Почему это плохо:**
1. Нарушает стандарты нумерации папок в корне проекта
2. Создает хаос в структуре (что это за папка? когда создана?)
3. Смешивает разные типы артефактов (исходный код vs метрики)
4. Противоречит `00_docs/standards/common/structure.md`

**Замечание пользователя:**
> "Разработчики при создании тестов отклоняются от стандартов, и создают папки, связанные с тестированием, прямо в корне проекта, типа папок tests, htmlcov и т.д. Мало того, что они без нумерации, так еще и в корне... Нужно установить правила, что они либо в соответствии со стандартами нумеруют папки в корне (т.е. 05_tests), либо делают их внутри 02_src (там можно без нумерации)."

**Как надо (два варианта):**

**Вариант A: Нумерованная папка в корне**
```
project/
├── 00_docs/
├── 01_tasks/
├── 02_src/
├── 03_data/
├── 04_logs/
├── 05_tests/              ✅ Нумерованная папка в корне
│   ├── unit/
│   ├── integration/
│   ├── fixtures/
│   └── htmlcov/           ✅ Метрики внутри 05_tests
└── README.md
```

**Вариант B: Внутри 02_src (без нумерации)**
```
project/
├── 00_docs/
├── 01_tasks/
├── 02_src/
│   ├── vlm_ocr_doc_reader/
│   │   ├── core/
│   │   ├── operations/
│   │   └── tests/         ✅ Тесты внутри модуля
│   └── tests/             ✅ Общие тесты (если нужны)
└── README.md
```

**Правило:**
- ✅ Если тесты в корне - нумеруй папку (`05_tests/`)
- ✅ Если тесты внутри `02_src/` - можно без нумерации
- ✅ Метрики (htmlcov, coverage.xml) - внутри папки тестов
- ❌ НЕ создавай `tests/`, `htmlcov/` и т.п. в корне проекта

**Рекомендация для стандартов:**
- Добавить в `00_docs/standards/common/structure.md` раздел про тестирование
- Явно указать два варианта размещения тестов
- Добавить в `.agents/developer.md` напоминание про структуру

---

## Рекомендации по совершенствованию стандартов

### Для `.agents/architect.md`

**Добавить в раздел "Принципы работы":**
```markdown
## Уровень детализации

1. **Стратегический уровень:** Ты работаешь на уровне системы (компоненты, взаимодействие, ограничения)
2. **НЕ детализируй реализацию:** Контракты методов, форматы данных - за Tech Lead
3. **Пиши для первого читателя:** Документы должны быть самодостаточными без контекста диалога
4. **Указывай источники:** Всегда упоминай проекты для изучения, напоминай про доступ
```

**Добавить в раздел "Что ты делаешь":**
```markdown
## Что ты НЕ делаешь

- ❌ Создаешь backlog (это за Tech Lead)
- ❌ Создаешь задачи (это за Tech Lead)
- ❌ Принимаешь работу (это за Tech Lead)
- ❌ Пишешь контракты методов в псевдокоде (это за Tech Lead)
- ❌ Пишешь "следующие шаги" для других ролей
```

---

### Для `.agents/tech-lead.md`

**Добавить в начало "Чек-лист старта":**
```markdown
## Перед началом работы

1. [ ] Прочитать описание своей роли
2. [ ] Изучить `overview.md` от Architect
3. [ ] **Проверить доступ к проектам-источникам** (указанным в overview.md)
4. [ ] Если доступа нет - запросить у пользователя
5. [ ] Изучить существующие паттерны в проектах-источниках
6. [ ] Переиспользовать, не изобретать заново
```

**Добавить новый раздел "Декомпозиция задач":**
```markdown
## Декомпозиция задач

### Принципы

- ❌ **НЕ дроби слишком мелко:** Каждая мелкая задача создает overhead
- ✅ **Укрупненные блоки:** One feature/major module - одна задача
- ✅ **Параллельная работа:** Analyst может запускать несколько Developer
- ✅ **Минимизируй handoff'ы:** Меньше передач = меньше контекстных потерь

### Когда дробить, а когда нет

**Дробить на части когда:**
- Независимые модули (можно делать параллельно)
- Разные команды работают
- Экспериментальные фичи (может не пригодиться)

**НЕ дробить когда:**
- Связанная логика одного модуля
- Результат parts нужно сводить вместе
- Создает больше overhead чем пользы

### Пример

❌ Плохо: 10 мелких задач на реализацию одного модуля
✅ Хорошо: 1 задача на модуль, Analyst запускает 2 Developer параллельно
```

---

### Для `00_docs/standards/architect/documentation.md` (новый файл)

```markdown
# Написание архитектурных документов

## Цель

Создавать самодостаточные документы, понятные без контекста диалога.

## Писать для первого читателя

- ❌ НЕ используй контекстные ссылки ("Вариант D", "как обсуждали")
- ✅ Объясняй выбор альтернативы если упоминаешь решение
- ✅ Предполагай что читатель не видел диалога с пользователем

## Уровень детализации

### Что делать (стратегический уровень)
- Компоненты системы
- Взаимодействие между компонентами
- Технологический стек и обоснование
- Ограничения и требования
- Интеграционные точки

### Что НЕ делать (уровень реализации)
- ❌ Псевдокод контрактов методов
- ❌ Детальная структура файлов
- ❌ Форматы данных на уровне полей
- ✅ Это за Tech Lead после изучения существующего кода

## Что НЕ должно быть в overview.md

- ❌ Backlog
- ❌ Задачи
- ❌ "Следующие шаги" для других ролей
- ❌ Детальные контракты методов
- ❌ Псевдокод реализации

## Что должно быть в оговорке для Tech Lead

- ✅ Список проектов-источников для изучения
- ✅ Напоминание запросить доступ если его нет
- ✅ Указание что это предложение, а не жесткий стандарт
```

---

### Для `00_docs/standards/tech-lead/decomposition.md` (новый файл)

```markdown
# Декомпозиция и планирование

## Принципы декомпозиции

### Укрупненные блоки

- ❌ НЕ дроби задачи слишком мелко
- ✅ Цель: укрупненные блоки работы (features, modules)
- ✅ Минимизировать overhead на передачу между агентами

### Работа с командой

- Analyst может запускать несколько Developer параллельно
- Reviewer может работать с крупными блоками
- Избегай ситуации когда каждая мелкая задача проходит полный цикл

## Перед реализацией

1. Изучи существующие проекты
2. Переиспользуй паттерны
3. Если нет доступа - запроси у пользователя
4. Не изобретай заново то что уже работает

## Чек-лис декомпозиции

Перед созданием задачи спроси себя:
- [ ] Это действительно независимая задача или часть целого?
- [ ] Создаст ли это избыточный overhead на handoff'ы?
- [ ] Может ли Analyst охватить этот блок целиком?
- [ ] Можно ли сделать параллельно с другими задачами?

Если ответы указывают на излишнее дробление - объедини с соседними задачами.
```

---

### Для `00_docs/standards/developer/structure.md` (новый файл)

```markdown
# Структура проекта и тестирование

## Размещение тестов

**Правило:** Соблюдай стандарты нумерации папок в корне проекта.

### Вариант A: Нумерованная папка в корне

```
project/
├── 00_docs/
├── 01_tasks/
├── 02_src/
├── 03_data/
├── 04_logs/
├── 05_tests/              ✅ Нумерованная папка в корне
│   ├── unit/
│   ├── integration/
│   ├── fixtures/
│   └── htmlcov/           ✅ Метрики внутри
└── README.md
```

### Вариант B: Внутри 02_src

```
project/
├── 00_docs/
├── 01_tasks/
├── 02_src/
│   ├── my_module/
│   │   ├── core/
│   │   └── tests/         ✅ Внутри модуля
│   └── tests/             ✅ Общие тесты
└── README.md
```

## Что НЕ делать

❌ **Запрещено в корне проекта:**
- `tests/` - без нумерации
- `htmlcov/` - метрики в корне
- `coverage.xml` - отчеты в корне
- `.pytest_cache/` - кэш в корне (должен быть в .gitignore)

✅ **Правильно:**
- `05_tests/` - нумерованная папка
- `05_tests/htmlcov/` - метрики внутри
- `02_src/my_module/tests/` - внутри модуля

## Чек-лист перед созданием папки тестов

- [ ] Папка в корне? → Добавь номер (`05_tests/`)
- [ ] Папка внутри `02_src/`? → Можно без нумерации
- [ ] Метрики (htmlcov)? → Только внутри папки тестов
- [ ] Добавил в .gitignore? → Кэш, временные файлы
```

---

## Заключение

Ключевые паттерны улучшения:

1. **Architect:** Стратегия, не реализация. Пиши для первого читателя.
2. **Tech Lead:** Укрупненные блоки, минимизация overhead. Изучи существующее до начала.
3. **Developer:** Соблюдай структуру проекта. Тесты - с нумерацией в корне или внутри 02_src.
4. **Документы:** Самодостаточные, без контекстных ссылок.
5. **Декомпозиция:** One feature - one task (где разумно).
6. **Переиспользование:** Изучи код до написания стандартов.

Эти замечания основаны на реальной работе и могут быть использованы для улучшения обучения агентов и стандартов.
